# Pi headphone output mode (installed to /etc/pi-ambient-synth/audio-mode.conf).
# Fallback: KeyStep → aplay blips (no SuperCollider). Use pi_enable_direct_keys.sh.
# Full engine: deploy/pi-audio-mode-ambient.conf + pi_enable_ambient_engine.sh
AUDIO_MODE=direct_keys
