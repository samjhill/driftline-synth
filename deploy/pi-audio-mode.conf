# Pi headphone output mode (installed to /etc/pi-ambient-synth/audio-mode.conf).
# Production default: KeyStep → OSC → SuperCollider (pi_enable_ambient_engine.sh).
# Fallback blips only: deploy/pi-audio-mode.conf via pi_enable_direct_keys.sh
# Experimental: deploy/pi-audio-mode-flues.conf + pi_enable_flues_engine.sh
AUDIO_MODE=ambient
