# Installed by pi_enable_ambient_engine.sh — KeyStep blips via aplay while SC runs on JACK.
[Service]
Environment=PI_HYBRID_ALSA_KEYS=1
Environment=PI_BLIP_ALSA_DEVICE=plughw:0,0
Environment=PI_BLIP_DURATION=0.32
